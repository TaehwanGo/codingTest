const printDiv = document.querySelector('.print');

/**
 * 자연수 N개가 중복없이 들어있는 배열이 있습니다. 
 * 이때, 서로 다른 두 원소의 위치를 바꾸는 Swap 연산을 이용해 원소들의 위치를 바꿔 
 * 서로 인접한 원소의 차가 K 이하가 되도록 하려 합니다. 
 * 단, Swap 연산을 최대한 적게 사용해야 합니다
배열 numbers가 매개변수로 주어질 때, 
서로 인접한 원소의 차가 K 이하가 되도록 하는데 필요한 Swap 횟수의 최솟값을 return 하도록 
solution 함수를 완성해주세요.
*/

/**
 * @param {*} numbers
 * @param {*} k
 */
function solution(numbers, k) {}

const answer = solution([10, 40, 30, 20], 20);

console.log(answer);

printDiv.innerHTML = answer;
